/**
 * Using this instead of <br /> because Tailwind's prose typography doesn't style <br /> elements.
 */

export const Spacer = () => (
  <div style={{ minHeight: '1rem', marginTop: 0, marginBottom: 0 }} />
)
